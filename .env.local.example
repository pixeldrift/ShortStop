# Copy this file to .env.local (gitignored) and fill in real values.
# .env.local is only read by scripts/geocodeRoute.ts (npm run geocode)
# right now - Next.js itself doesn't need any env vars yet.

# Free key from https://openrouteservice.org/dev/#/signup - used for
# geocoding route-125.csv's waypoints (see src/lib/geocode.ts) and,
# eventually, the routing/directions call.
ORS_API_KEY=
